# 怪咖日记 (Weirdo Diary) - 纯移动 App 项目使用说明

怪咖日记已成功重构为**离线优先 (Offline-First) 的纯移动 App 架构**。本项目由两部分组成：运行于手机端的 Expo React Native App，以及作为 AI 处理和用户登录代理的 Next.js 服务端。

---

## 1. 项目核心概述

### 1.1 设计与技术架构

```
┌─────────────────────────┐   HTTPS (Auth + AI)   ┌─────────────────────────┐
│  React Native (Expo)    │ ◄────────────────────► │  Next.js API 代理服务器 │
│  weirdo-diary-app/      │                        │  (原项目，精简保留)      │
│                         │                        │                          │
│  📱 本地沙盒存储:        │                        │  🔐 接口鉴权:            │
│     expo-file-system     │                        │     Supabase Auth JWT   │
│     expo-secure-store    │                        │                         │
│                         │                        │  🤖 AI 代理中转:          │
│  📦 离线归档备份:        │                        │     Gemini Multi-modal  │
│     JSZip HTML 打包      │                        │     TTS 流式转换 pipeline │
└─────────────────────────┘                        └─────────────────────────┘
```

* **客户端 (weirdo-diary-app)**：
  * **技术栈**：Expo SDK 54, React Native, Zustard (状态管理), JSZip (本地归档), Supabase Client (本地 SecureStore 存储 Token 鉴权)。
  * **离线数据**：日记文本、时间、手账标签、用户语音录音 (.wav)、日记封面图片等均 100% 存储于手机本地沙盒，绝对不上云，最大程度保障隐私。
  * **手账美学**：采用精心调配的复古手账界面风格（米黄 `#f5f0e8`、深棕 `#2c1810` 和陶土红 `#c6604a`），提供手绘风语音波形、大字机动效等。
  * **离线备份**：支持一键导出离线 ZIP 压缩包（内含精美 HTML 本地日记播放器，可在任意电脑浏览器中直接阅读和播放录音）与自包含 Markdown 文件。
  * **预设标签管理**：系统设置页支持自定义增加、删除事件预设和情绪状态，并保存于本地。

* **服务端 (Next.js)**：
  * 作为安全边界代理，处理多模态视觉图像识别、日记助手对话流、ASR 语音转文字、TTS 语音合成等，对客户端请求进行 Supabase Bearer JWT 校验，配置了完整的 CORS 跨域请求支持。

---

## 2. 目录结构说明

```
AI_live_diary/
├── weirdo-diary-app/           # 📱 移动端客户端 App 根目录 (Expo SDK 54)
│   ├── app/                    # Expo Router 页面文件 (含 tabs 路由与详情页)
│   ├── components/             # 复古手绘风 UI 组件 (VoiceWave, MessageBubble 等)
│   ├── lib/                    # 核心逻辑 (storage.ts 本地存储, export.ts 导出等)
│   ├── tsconfig.json           # 移动端 TypeScript 配置
│   └── package.json            # 移动端依赖列表 (支持 SDK 54 与 @expo/ngrok 隧道)
│
├── app/                        # 🤖 Next.js 后端服务路由 (AI 视觉、流式 TTS 中转等)
│   └── api/
│       ├── auth/profile/       # 登录鉴权同步端点
│       └── diaries/            # 视觉识别、语音分析、AI 聊天及 Stream-TTS
├── lib/                        # 服务端公用方法 (verifyAuth 鉴权、CORS 头处理等)
├── tsconfig.json               # 服务端 TypeScript 配置 (已排除移动端，避免冲突)
└── package.json                # 后端服务依赖
```

---

## 3. 运行指南

### 步骤一：启动后端代理服务 (Next.js)

1. 打开一个终端窗口。
2. 确保进入项目根目录 `D:\codex_projects\AI_live_diary`。
3. 运行开发命令：
   ```bash
   npm run dev
   ```
4. 后端服务将默认启动在 `http://localhost:3000`。
   > **提示**：运行前请确保根目录下 `.env.local` 环境变量配置文件中，已正确配置 Supabase 项目公钥与 AI 模型服务密钥。

### 步骤二：启动移动端客户端 (Expo React Native)

1. 打开另一个独立的终端窗口。
2. 进入移动端子目录 `weirdo-diary-app`：
   ```bash
   cd weirdo-diary-app
   ```
3. 启动 Expo Metro 开发服务器（推荐使用**隧道模式**，可避开手机与电脑必须在同一 Wi-Fi 下的局域网限制）：
   ```bash
   npx expo start --tunnel
   ```
4. 控制台启动完毕后，将输出一个专属的二维码。
5. **手机扫码预览**：
   * **Android 手机**：在 Google Play 商店或 Expo 官网下载 **Expo Go** 客户端 App，打开后使用内置扫码功能扫描终端生成的二维码。
   * **iPhone 手机**：直接打开系统自带相机，扫描二维码后选择通过 **Expo Go** 打开预览。

---

## 4. 本地维护与类型校验

项目已经配置好严格的类型防错校验。如果在开发中做出了修改，可在对应的目录下执行编译验证，确保没有任何编译警告和语法错误：

* **校验移动端代码**：
  在 `weirdo-diary-app` 目录下运行：
  ```bash
  npx tsc --noEmit
  ```

* **校验后端服务代码**：
  在项目根目录下运行：
  ```bash
  npm run typecheck
  ```
